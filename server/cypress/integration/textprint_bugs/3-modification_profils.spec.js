/// <reference types="Cypress" />

context('Modification de profils', () => {
    /*
    it('Création d\'un profil connu sans ajouter de nom', () => {
        
        cy.get('#creer-profil-connu')
          .click({ force: true })

        cy.get('#prenom')
          .type('Paul', { delay: 100 }).should('have.value', 'Paul')
        
        cy.get('#age')
          .type('22', { delay: 100 }).should('have.value', '22')
        
        cy.get('#sexe')
          .select('femme')

        cy.get('#sociale')
          .select('aisee')

        cy.get('#commentaire')
          .type('Ceci est un commentaire pour Paul...', { delay: 100 }).should('have.value', 'Ceci est un commentaire pour Paul...')

        cy.get('#btn-creer-profil-connu')
          .click({ force: true })

        cy.on('window:alert', (str) => {
            expect(str).contains(`Une erreur s'est produite.`)
        })
          
      })

      it('Création d\'un profil connu sans ajouter de prénom', () => {

        cy.get('#prenom')
          .clear()

        cy.get('#nom')
          .type('Durand', { delay: 100 }).should('have.value', 'Durand')
        
        cy.get('#btn-creer-profil-connu')
          .click({ force: true })

        cy.on('window:alert', (str) => {
            expect(str).contains(`Une erreur s'est produite.`)
        })
          
      })


      it('Création d\'un profil avec toutes les informations connues', () => {

        cy.get('#prenom')
          .type('Paul', { delay: 100 }).should('have.value', 'Paul')
        
        cy.get('#creer-profil-connu')
          .click({ force: true })

        cy.get('#btn-creer-profil-connu')
          .click({ force: true })

        cy.on('window:alert', (str) => {
            expect(str).contains(`a bien été crée`)
        })
          
      })

      it('Création d\'un profil anonyme', () => {

        cy.get('#prenom')
          .type('Paul', { delay: 100 }).should('have.value', 'Paul')
        
        cy.get('#creer-profil-connu')
          .click({ force: true })

        cy.get('#btn-creer-profil-connu')
          .click({ force: true })

        cy.on('window:alert', (str) => {
            expect(str).contains(`a bien été crée`)
        })
          
      })
      */

})
